export enum ErrorType {
  CLIENT = 'CLIENT',
  SERVER = 'SERVER',
  CRITICAL = 'CRITICAL',
}

export enum ErrorResolutionStatus {
  ALL = 'ALL',
  RESOLVED = 'RESOLVED',
  UNRESOLVED = 'UNRESOLVED',
}
